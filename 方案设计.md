# 方案设计

## 一、功能需求分析

### 1.1 系统总体功能

本系统面向工业旋转机械轴承的智能故障诊断需求，提供从振动信号上传、预处理、特征提取、故障分类到健康度评估的全流程智能化服务。系统采用B/S架构，用户通过浏览器即可完成全部操作，无需安装客户端软件。

### 1.2 功能模块划分

系统划分为**四大功能模块**，各模块功能需求如下：

#### 模块一：数据管理模块

| 功能点 | 需求描述 | 优先级 |
|--------|----------|--------|
| 数据上传 | 支持CSV、TXT格式的振动信号文件上传，单文件最大100MB | 高 |
| 数据预览 | 上传后自动解析信号，展示波形图、频谱图、基本统计信息 | 高 |
| 数据预处理 | 支持小波降噪、带通滤波、中值滤波、归一化等预处理操作，可配置参数 | 高 |
| 数据集管理 | 查看已上传数据集列表，支持删除、重命名、查看详情 | 中 |
| 数据统计 | 展示数据集中各故障类别的样本数量分布 | 中 |

#### 模块二：在线诊断模块

| 功能点 | 需求描述 | 优先级 |
|--------|----------|--------|
| 单文件诊断 | 上传单个振动信号文件，自动完成预处理→特征提取→故障分类→健康度评估全流程 | 高 |
| 诊断结果展示 | 展示预测故障类型、置信度、各类别概率分布、健康度评分、异常检测结果 | 高 |
| 信号可视化 | 展示原始信号波形、预处理后波形、频谱图、包络谱图 | 高 |
| 特征展示 | 展示提取的41维特征数值，按重要性排序 | 中 |
| 批量诊断 | 支持上传ZIP压缩包进行批量诊断，输出汇总报告 | 低 |

#### 模块三：模型管理模块

| 功能点 | 需求描述 | 优先级 |
|--------|----------|--------|
| 模型训练 | 选择数据集和模型类型（SVM/随机森林/KNN/决策树/GBDT），配置超参数进行训练 | 高 |
| 模型评估 | 展示各模型的准确率、精确率、召回率、F1分数、混淆矩阵、交叉验证结果 | 高 |
| 模型对比 | 多模型横向对比，自动选择F1最高的模型作为默认诊断模型 | 高 |
| 特征重要性 | 基于随机森林展示特征重要性排序条形图 | 中 |
| 模型切换 | 支持手动切换在线诊断使用的模型 | 中 |

#### 模块四：仪表盘与历史记录模块

| 功能点 | 需求描述 | 优先级 |
|--------|----------|--------|
| 系统概览 | 展示诊断总次数、异常检出率、各故障类型分布、模型准确率等核心指标 | 高 |
| 历史记录 | 展示历史诊断记录列表，支持按时间、故障类型、健康度筛选 | 高 |
| 记录详情 | 查看单次诊断的完整信息，包括信号图、特征值、诊断结果 | 中 |
| 趋势分析 | 展示健康度随时间变化趋势图，支持退化趋势预测 | 中 |
| 数据导出 | 支持诊断记录导出为CSV格式 | 低 |

### 1.3 非功能需求

| 类别 | 需求描述 | 指标 |
|------|----------|------|
| 性能 | 单文件诊断响应时间 | < 2秒（10万点信号） |
| 性能 | 页面加载时间 | < 3秒 |
| 性能 | 模型训练时间 | < 30秒（1000样本） |
| 可靠性 | 系统可用性 | > 99% |
| 可靠性 | 数据持久化 | 诊断记录、模型信息持久化存储 |
| 易用性 | 界面设计 | 简洁直观，操作流程不超过3步 |
| 易用性 | 响应式布局 | 支持1920×1080及以上分辨率 |
| 兼容性 | 浏览器支持 | Chrome、Edge、Firefox最新版本 |
| 安全性 | 文件上传 | 限制文件类型和大小，防止恶意文件上传 |
| 可维护性 | 代码规范 | 模块化设计，注释完整，遵循PEP8规范 |
| 可测试性 | 自动化测试 | 核心算法模块和API接口覆盖率>80% |

---

## 二、方案论证

### 2.1 技术路线选型论证

#### （1）B/S架构 vs C/S架构

| 对比维度 | B/S架构 | C/S架构 |
|----------|---------|---------|
| 部署方式 | 浏览器访问，无需安装客户端 | 需安装客户端软件 |
| 维护成本 | 服务器端统一维护，升级方便 | 需逐台升级客户端 |
| 跨平台性 | 支持Windows/macOS/Linux | 需针对不同平台开发 |
| 性能 | 受网络带宽限制 | 本地运算，性能更好 |
| 适用场景 | 多用户、分布式访问 | 单机、高性能计算 |

**选型结论**：采用**B/S架构**。理由：①课程设计要求可现场演示，B/S架构通过浏览器即可访问，演示方便；②系统面向多用户场景（运维人员、管理人员），B/S架构更适合；③前后端分离，便于开发和维护；④振动信号诊断计算量适中，服务器端可满足性能要求。

#### （2）后端框架选型

| 框架 | 语言 | 优势 | 劣势 |
|------|------|------|------|
| Flask | Python | 轻量灵活、生态丰富、AI库支持好、学习成本低 | 大型项目需自行组装 |
| Django | Python | 全栈框架、自带ORM和Admin | 偏重，灵活性差 |
| Spring Boot | Java | 企业级、性能好、生态成熟 | 与AI算法集成复杂 |
| FastAPI | Python | 高性能、自动生成API文档 | 生态相对较新 |

**选型结论**：采用**Flask**。理由：①Python生态在机器学习领域有压倒性优势（scikit-learn、scipy、numpy等）；②Flask轻量灵活，适合课程设计项目规模；③团队熟悉Python语言，开发效率高；④Flask-SQLAlchemy、Flask-CORS等扩展可满足项目需求。

#### （3）前端技术选型

| 方案 | 优势 | 劣势 |
|------|------|------|
| 原生HTML/CSS/JS + ECharts | 无需构建工具、部署简单、学习成本低 | 复杂交互开发效率低 |
| Vue.js + Element UI | 组件化开发、生态丰富、界面美观 | 需Node.js构建环境 |
| React + Ant Design | 性能好、社区活跃 | 学习曲线陡峭 |

**选型结论**：采用**原生HTML/CSS/JavaScript + ECharts**。理由：①课程设计项目规模适中，原生开发足够应对；②无需额外构建工具，降低环境配置复杂度；③ECharts是百度开源的可视化库，功能强大，适合信号波形、频谱、混淆矩阵等可视化需求；④前后端通过RESTful API交互，耦合度低。

#### （4）数据库选型

| 数据库 | 类型 | 优势 | 劣势 |
|--------|------|------|------|
| SQLite | 关系型 | 零配置、单文件、易部署、Python内置支持 | 并发性能有限 |
| MySQL | 关系型 | 企业级、并发性能好 | 需安装配置 |
| MongoDB | 文档型 | 灵活Schema、适合非结构化数据 | 事务支持弱 |

**选型结论**：采用**SQLite**。理由：①课程设计项目数据量小（诊断记录、模型信息），SQLite完全满足；②零配置，单文件数据库，便于部署和迁移；③Python内置sqlite3模块，Flask-SQLAlchemy完美支持；④演示环境无需额外安装数据库服务。

### 2.2 算法方案论证

#### （1）故障诊断方法选型

轴承故障诊断方法主要分为三类：

| 方法类别 | 代表方法 | 优势 | 劣势 |
|----------|----------|------|------|
| 基于信号处理 | 包络分析、小波分析 | 物理意义明确、无需训练数据 | 依赖专家经验、自动化程度低 |
| 基于机器学习 | SVM、随机森林、神经网络 | 自动化程度高、可处理高维特征 | 需要标注数据、特征工程影响大 |
| 基于深度学习 | CNN、LSTM、Transformer | 端到端学习、自动特征提取 | 数据需求量大、可解释性差、计算资源要求高 |

**选型结论**：采用**"信号处理 + 传统机器学习"**的混合方案。理由：①课程设计要求运用至少3个课程技术方向，信号处理和机器学习分别对应不同章节；②公开轴承数据集样本量有限（通常每类几百到几千样本），传统机器学习比深度学习更适合小样本场景；③传统机器学习模型可解释性强，便于答辩时讲解代码逻辑；④信号处理（包络分析、小波降噪）是轴承故障诊断的经典方法，工程价值高。

#### （2）分类模型选型

选择5种经典分类模型进行对比：

| 模型 | 原理 | 适用场景 | 在本项目中的优势 |
|------|------|----------|-----------------|
| SVM | 最大间隔超平面 | 小样本、高维 | 适合41维特征、小样本场景，泛化能力强 |
| 随机森林 | 多决策树集成 |  tabular数据 | 抗过拟合、可输出特征重要性、鲁棒性好 |
| KNN | 近邻投票 | 简单分类 | 实现简单、无需训练、可解释性强 |
| 决策树 | 树形决策 | 规则提取 | 可解释性最强、便于理解分类逻辑 |
| GBDT | 梯度提升树 | 高精度分类 | 迭代优化、精度高、适合结构化数据 |

**选型结论**：同时实现5种模型，训练后自动选择F1分数最高的作为默认模型。理由：①多模型对比可验证方案的鲁棒性；②不同模型有不同的可解释性和性能特点，便于答辩展示；③随机森林可输出特征重要性，增强系统可解释性。

### 2.3 数据集方案论证

#### （1）数据集来源

| 数据集 | 来源 | 数据规模 | 故障类型 | 采样率 |
|--------|------|----------|----------|--------|
| CWRU轴承数据集 | 凯斯西储大学 | 每类约2400样本 | 正常、内圈、外圈、滚动体 | 12kHz/48kHz |
| 和鲸社区轴承数据集 | 和鲸社区 | 待确认 | 正常、内圈、外圈、滚动体 | 待确认 |
| 模拟生成数据集 | 本项目生成 | 每类500样本 | 正常、内圈、外圈、滚动体 | 12kHz |

**选型结论**：采用**"公开数据集 + 模拟生成数据集"**双轨方案。理由：①公开数据集（CWRU）是轴承故障诊断领域的标准数据集，研究成果可对比；②若公开数据集下载受限，模拟生成数据集可保证系统正常运行和演示；③模拟数据基于轴承故障振动信号的数学模型生成，具有物理合理性；④data目录中已有的data_intro.md指向和鲸社区数据集，可作为补充数据源。

#### （2）模拟数据生成原理

轴承故障振动信号的数学模型：

$$x(t) = \sum_{i=1}^{n} A_i \cdot e^{-\xi \omega_n (t-t_i)} \cdot \sin(\omega_d \sqrt{(t-t_i)^2 - \tau^2}) \cdot u(t-t_i) + n(t)$$

其中：
- $A_i$：第i次冲击的幅值
- $\xi$：阻尼比
- $\omega_n$：固有频率
- $\omega_d$：阻尼固有频率
- $t_i$：第i次冲击发生时间（故障特征频率的倒数）
- $n(t)$：高斯白噪声

不同故障类型对应不同的故障特征频率：
- 内圈故障：$BPFI = \frac{1}{2} f_r \cdot n_b \cdot (1 + \frac{d}{D} \cos\alpha)$
- 外圈故障：$BPFO = \frac{1}{2} f_r \cdot n_b \cdot (1 - \frac{d}{D} \cos\alpha)$
- 滚动体故障：$BSF = \frac{1}{2} f_r \cdot \frac{D}{d} \cdot (1 - (\frac{d}{D} \cos\alpha)^2)$

---

## 三、方案规划

### 3.1 系统总体架构

系统采用**经典三层B/S架构**，自下而上分为：

```
┌─────────────────────────────────────────────────────────┐
│                     表现层（前端）                         │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐  │
│  │ 仪表盘页 │ │ 在线诊断 │ │ 数据管理 │ │ 模型管理 │  │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘  │
│         HTML/CSS/JavaScript + ECharts可视化               │
├─────────────────────────────────────────────────────────┤
│                     业务逻辑层（后端）                      │
│  ┌─────────────────────────────────────────────────────┐ │
│  │              Flask RESTful API 路由层                 │ │
│  ├─────────────────────────────────────────────────────┤ │
│  │  诊断服务  │  数据服务  │  模型服务  │  系统服务    │ │
│  ├─────────────────────────────────────────────────────┤ │
│  │                   算法引擎层                           │ │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌────────┐ │ │
│  │  │ 数据预处 │ │ 特征提取 │ │ 故障分类 │ │ 异常检 │ │ │
│  │  │   理     │ │          │ │          │ │  测    │ │ │
│  │  └──────────┘ └──────────┘ └──────────┘ └────────┘ │ │
│  └─────────────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────────────┤
│                     数据访问层                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │   SQLite     │  │  模型文件    │  │  上传文件    │  │
│  │  (诊断记录)  │  │ (joblib/pkl) │  │  (uploads)   │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└─────────────────────────────────────────────────────────┘
```

### 3.2 技术栈详细规划

#### 后端技术栈

| 层次 | 技术/库 | 版本 | 用途 |
|------|---------|------|------|
| Web框架 | Flask | 3.0.0 | 提供RESTful API服务 |
| 跨域 | Flask-CORS | 4.0.0 | 处理前后端跨域请求 |
| ORM | Flask-SQLAlchemy | 3.1.1 | 数据库对象关系映射 |
| 数值计算 | NumPy | 1.26.2 | 数组运算、信号处理基础 |
| 数据处理 | Pandas | 2.1.3 | CSV文件读取、数据框操作 |
| 信号处理 | SciPy | 1.11.4 | FFT、滤波、希尔伯特变换、统计分析 |
| 机器学习 | scikit-learn | 1.3.2 | 分类模型、特征标准化、模型评估、交叉验证 |
| 小波变换 | PyWavelets | 1.5.0 | 小波降噪、小波包分解 |
| 模型持久化 | joblib | 1.3.2 | 模型序列化与反序列化 |
| 可视化 | Matplotlib | 3.8.2 | 后端生成图表（可选） |

#### 前端技术栈

| 技术/库 | 版本 | 用途 |
|---------|------|------|
| HTML5 | - | 页面结构 |
| CSS3 | - | 页面样式，Flexbox/Grid布局 |
| JavaScript (ES6+) | - | 交互逻辑、API调用 |
| ECharts | 5.4.3 | 信号波形图、频谱图、混淆矩阵、仪表盘图表 |
| Fetch API | - | 前后端数据交互 |

#### 开发与运维工具

| 工具 | 用途 |
|------|------|
| Git | 版本控制，小步提交 |
| pip | Python包管理 |
| pytest | 自动化测试框架 |
| VS Code / Cursor | 代码编辑器 |
| AI编程助手 | vibe coding辅助开发 |

### 3.3 外部资源配置

| 资源类型 | 具体内容 | 配置方式 | 必要性 |
|----------|----------|----------|--------|
| Python环境 | Python 3.9+ | 本地安装Anaconda或官方Python | 必需 |
| Python依赖包 | requirements.txt中列出的12个包 | pip install -r requirements.txt | 必需 |
| 前端CDN资源 | ECharts 5.4.3 | 通过CDN引入（jsdelivr） | 必需（也可下载到本地） |
| 轴承振动数据集 | CWRU数据集 / 和鲸社区数据集 / 模拟生成 | 下载到data目录或运行生成脚本 | 必需 |
| 浏览器 | Chrome / Edge 最新版 | 本地已安装 | 必需 |
| 数据库 | SQLite | Python内置，无需额外安装 | 必需 |
| GPU | 无 | 传统机器学习无需GPU | 非必需 |

### 3.4 数据集来源规划

| 序号 | 数据集名称 | 来源 | 格式 | 用途 | 状态 |
|------|-----------|------|------|------|------|
| 1 | CWRU轴承数据集 | 凯斯西储大学官网 | .mat | 模型训练与测试 | 备选（需下载转换） |
| 2 | 和鲸社区轴承数据集 | https://www.heywhale.com/mw/dataset/69a7d5b64a5a2f8d77925643 | CSV | 模型训练与测试 | 备选（需注册下载） |
| 3 | 模拟生成数据集 | 本项目scripts/generate_dataset.py生成 | CSV | 系统演示、模型训练 | 已规划，将生成 |

**数据集字段设计**（模拟生成的CSV格式）：

| 列名 | 类型 | 说明 |
|------|------|------|
| sample_id | int | 样本编号 |
| fault_type | int | 故障类型标签（0=正常,1=内圈,2=外圈,3=滚动体） |
| fault_name | str | 故障类型名称 |
| load_condition | float | 负载条件（HP） |
| sample_0 ~ sample_N | float | 振动信号采样点（N=1024或更多） |

### 3.5 前端UI方案

#### 页面结构规划

系统共设计**5个核心页面**，采用统一的侧边栏导航布局：

```
┌──────────────────────────────────────────────────────┐
│  顶部导航栏：系统标题 │ 当前用户 │ 系统状态            │
├────────┬─────────────────────────────────────────────┤
│        │                                             │
│ 侧边栏 │              主内容区域                       │
│        │                                             │
│ 仪表盘 │  （根据当前页面动态渲染）                     │
│ 在线诊断│                                             │
│ 数据管理│                                             │
│ 模型管理│                                             │
│ 历史记录│                                             │
│        │                                             │
└────────┴─────────────────────────────────────────────┘
```

#### 各页面详细设计

**页面1：仪表盘（Dashboard）**
- 顶部：4个核心指标卡片（诊断总次数、异常检出率、平均健康度、模型准确率）
- 中部左侧：故障类型分布饼图
- 中部右侧：近7天诊断趋势折线图
- 底部：健康度分布柱状图 + 最近诊断记录表格

**页面2：在线诊断（Diagnosis）**
- 上部：文件上传区域（拖拽上传 + 点击上传）
- 中部左侧：信号可视化（原始波形/预处理波形/频谱/包络谱 4个Tab切换）
- 中部右侧：诊断结果面板（故障类型、置信度、概率分布条形图、健康度仪表盘、异常检测结果）
- 下部：特征值表格（41维特征，按重要性排序）

**页面3：数据管理（Data）**
- 上部：数据集上传 + 数据集列表
- 中部：选中数据集的详细信息（样本数、特征数、类别分布）
- 下部：数据集预览（前N行数据表格 + 波形图预览）

**页面4：模型管理（Model）**
- 上部：模型训练配置区（选择数据集、选择模型类型、超参数配置）
- 中部：模型评估结果（5种模型对比表格 + 混淆矩阵热力图）
- 下部：特征重要性条形图 + 模型操作按钮（设为默认、删除、重新训练）

**页面5：历史记录（History）**
- 上部：筛选条件（时间范围、故障类型、健康度范围）
- 中部：诊断记录表格（时间、文件名、故障类型、置信度、健康度、操作）
- 下部：选中记录的详情展示

#### UI设计风格
- **配色方案**：工业科技风，主色调深蓝色（#1890ff）+ 深灰背景（#f0f2f5）+ 白色卡片
- **字体**：系统默认无衬线字体，标题加粗
- **图表**：ECharts默认主题，配色协调
- **交互**：按钮hover效果、加载动画、结果渐入动画

### 3.6 后端算法方案

#### 算法模块架构

```
┌─────────────────────────────────────────────────┐
│              诊断服务（DiagnoseService）           │
│  调用流程：预处理 → 特征提取 → 故障分类 → 异常检测  │
├─────────────────────────────────────────────────┤
│                                                   │
│  ┌─────────────┐    ┌─────────────┐            │
│  │ 预处理模块   │───→│ 特征提取模块 │            │
│  │ Preprocessor│    │  FeatureExt │            │
│  └─────────────┘    └──────┬──────┘            │
│                              │                    │
│                    ┌─────────┴─────────┐         │
│                    ▼                   ▼         │
│             ┌───────────┐      ┌───────────┐   │
│             │ 故障分类   │      │ 异常检测   │   │
│             │ Classifier│      │ AnomalyDet│   │
│             └───────────┘      └───────────┘   │
│                                                   │
└─────────────────────────────────────────────────┘
```

#### 各算法模块详细方案

**模块1：数据预处理（preprocessing.py）**
- 输入：原始振动信号数组
- 处理步骤：异常值去除（3σ）→ 降噪（小波/中值/带通可选）→ 归一化（Z-score/MinMax/MaxAbs可选）→ 分帧
- 输出：预处理后信号、处理统计信息
- 关键参数：采样率12000Hz、帧长1024、帧移512

**模块2：特征提取（feature_extraction.py）**
- 输入：单帧信号（1024点）
- 提取41维特征：
  - 时域特征（16维）：均值、标准差、RMS、峰值、峰峰值、偏度、峭度、方差、峰值因子、脉冲因子、裕度因子、波形因子、能量、对数能量
  - 频域特征（13维）：频谱重心、频谱方差、频谱峭度、频谱熵、主频、主频率能量比、5个频带能量比
  - 时频域特征（8维）：小波包3层分解8个频带能量比 + 能量熵
  - 包络谱特征（4维）：包络谱峰值频率、峰值幅值、包络谱峭度
- 输出：特征字典、特征名列表、特征值数组

**模块3：故障分类（fault_classifier.py）**
- 输入：特征矩阵 + 标签
- 5种模型：SVM、随机森林、KNN、决策树、GBDT
- 训练流程：标签编码 → 7:3分层划分 → 标准化 → 模型训练 → 评估 → 交叉验证 → 选择最优模型
- 预测输出：故障类型、置信度、各类别概率分布
- 模型持久化：joblib序列化保存

**模块4：异常检测（anomaly_detection.py）**
- SPC异常检测：基于正常数据建立3σ控制限，检测RMS和峭度超限
- 孤立森林异常检测：基于正常特征训练，输出异常分数
- 健康度评估：综合RMS（40%）、峭度（35%）、峰值因子（25%），输出0-100分和5级等级
- 退化趋势预测：线性回归外推，预测未来健康度和失效时间

### 3.7 数据库设计

#### 数据表设计

**表1：诊断记录表（diagnosis_records）**

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | Integer | PRIMARY KEY, AUTOINCREMENT | 记录ID |
| file_name | String(255) | NOT NULL | 上传文件名 |
| fault_type | String(100) | NOT NULL | 预测故障类型名称 |
| fault_label | Integer | DEFAULT 0 | 故障类型标签 |
| confidence | Float | DEFAULT 0.0 | 预测置信度 |
| health_score | Float | DEFAULT 100.0 | 健康度评分 |
| health_level | String(50) | DEFAULT '优秀' | 健康等级 |
| is_anomaly | Boolean | DEFAULT FALSE | 是否异常 |
| anomaly_score | Float | DEFAULT 0.0 | 异常分数 |
| model_used | String(50) | DEFAULT 'random_forest' | 使用的模型 |
| signal_length | Integer | DEFAULT 0 | 信号长度 |
| sample_rate | Integer | DEFAULT 12000 | 采样率 |
| feature_summary | Text | | 特征摘要（JSON） |
| created_at | DateTime | DEFAULT NOW | 创建时间 |

**表2：数据集信息表（datasets）**

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | Integer | PRIMARY KEY | 数据集ID |
| name | String(255) | NOT NULL | 数据集名称 |
| file_path | String(500) | NOT NULL | 文件路径 |
| file_size | Integer | DEFAULT 0 | 文件大小（字节） |
| n_samples | Integer | DEFAULT 0 | 样本数 |
| n_features | Integer | DEFAULT 0 | 特征数 |
| n_classes | Integer | DEFAULT 0 | 类别数 |
| class_distribution | Text | | 类别分布（JSON） |
| description | Text | | 描述 |
| created_at | DateTime | DEFAULT NOW | 创建时间 |

**表3：模型信息表（models）**

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | Integer | PRIMARY KEY | 模型ID |
| model_type | String(50) | NOT NULL | 模型类型 |
| model_name | String(100) | NOT NULL | 模型名称 |
| accuracy | Float | DEFAULT 0.0 | 准确率 |
| precision | Float | DEFAULT 0.0 | 精确率 |
| recall | Float | DEFAULT 0.0 | 召回率 |
| f1_score | Float | DEFAULT 0.0 | F1分数 |
| cv_score | Float | DEFAULT 0.0 | 交叉验证分数 |
| n_train | Integer | DEFAULT 0 | 训练样本数 |
| n_test | Integer | DEFAULT 0 | 测试样本数 |
| n_features | Integer | DEFAULT 0 | 特征数 |
| n_classes | Integer | DEFAULT 0 | 类别数 |
| is_best | Boolean | DEFAULT FALSE | 是否最优模型 |
| trained_at | DateTime | DEFAULT NOW | 训练时间 |

**表4：系统日志表（system_logs）**

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | Integer | PRIMARY KEY | 日志ID |
| level | String(20) | DEFAULT 'INFO' | 日志级别 |
| module | String(50) | | 模块名 |
| message | Text | | 日志内容 |
| created_at | DateTime | DEFAULT NOW | 创建时间 |

### 3.8 API接口设计

#### 诊断相关接口

| 方法 | 路径 | 功能 | 请求参数 | 返回 |
|------|------|------|----------|------|
| POST | /api/diagnose/upload | 上传信号文件并诊断 | file (multipart) | 诊断结果JSON |
| GET | /api/diagnose/record/<id> | 获取单条诊断记录 | id | 记录详情 |
| GET | /api/diagnose/records | 获取诊断记录列表 | page, size, fault_type | 记录列表+分页 |
| DELETE | /api/diagnose/record/<id> | 删除诊断记录 | id | 操作结果 |
| GET | /api/diagnose/stats | 获取诊断统计信息 | - | 统计数据 |

#### 数据管理接口

| 方法 | 路径 | 功能 | 请求参数 | 返回 |
|------|------|------|----------|------|
| POST | /api/data/upload | 上传数据集 | file, name, description | 数据集信息 |
| GET | /api/data/list | 获取数据集列表 | page, size | 数据集列表 |
| GET | /api/data/<id> | 获取数据集详情 | id | 数据集详情 |
| DELETE | /api/data/<id> | 删除数据集 | id | 操作结果 |
| GET | /api/data/<id>/preview | 数据集预览 | id, rows | 预览数据 |

#### 模型管理接口

| 方法 | 路径 | 功能 | 请求参数 | 返回 |
|------|------|------|----------|------|
| POST | /api/model/train | 训练模型 | dataset_id, model_types, params | 训练结果 |
| GET | /api/model/list | 获取模型列表 | - | 模型列表 |
| GET | /api/model/<id> | 获取模型详情 | id | 模型详情 |
| POST | /api/model/<id>/select | 设为默认模型 | id | 操作结果 |
| GET | /api/model/feature-importance | 获取特征重要性 | - | 特征重要性列表 |
| DELETE | /api/model/<id> | 删除模型 | id | 操作结果 |

#### 系统接口

| 方法 | 路径 | 功能 | 请求参数 | 返回 |
|------|------|------|----------|------|
| GET | /api/system/info | 获取系统信息 | - | 系统状态、版本、模型状态 |
| GET | /api/system/health | 健康检查 | - | 200 OK |

---

## 四、技术路线

### 4.1 总体技术路线

```
需求分析 → 方案设计 → 数据准备 → 算法开发 → 后端开发 → 前端开发 → 集成测试 → 文档撰写 → 答辩准备
```

### 4.2 分阶段技术路线

#### 阶段1：数据准备与预处理（技术方向1）

```
原始振动信号
    │
    ▼
┌─────────────┐
│  数据加载    │  CSV/TXT读取、格式解析
└──────┬──────┘
       ▼
┌─────────────┐
│  异常值处理  │  3σ原则检测、中位数填充
└──────┬──────┘
       ▼
┌─────────────┐
│  信号降噪    │  小波降噪(db4,4层) / 带通滤波 / 中值滤波
└──────┬──────┘
       ▼
┌─────────────┐
│  归一化      │  Z-score / MinMax / MaxAbs
└──────┬──────┘
       ▼
┌─────────────┐
│  分帧        │  帧长1024、帧移512
└──────┬──────┘
       ▼
  预处理后信号
```

#### 阶段2：特征工程（技术方向2）

```
预处理后信号（单帧1024点）
    │
    ├──→ 时域统计分析 → 16维时域特征
    │
    ├──→ FFT频谱分析 → 13维频域特征
    │
    ├──→ 小波包分解(3层) → 8维时频域特征
    │
    └──→ 希尔伯特变换+包络谱 → 4维包络特征
              │
              ▼
        41维特征向量
```

#### 阶段3：故障分类（技术方向3）

```
41维特征矩阵 + 标签
    │
    ▼
┌─────────────┐
│ 数据划分     │  7:3分层抽样
└──────┬──────┘
       ▼
┌─────────────┐
│ 特征标准化   │  StandardScaler
└──────┬──────┘
       ▼
┌─────────────────────────────────────────┐
│           多模型并行训练                  │
│  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐ ┌────┐│
│  │ SVM │ │ RF  │ │ KNN │ │ DT  │ │GBDT││
│  └─────┘ └─────┘ └─────┘ └─────┘ └────┘│
└───────────────────┬─────────────────────┘
                     ▼
┌─────────────────────────────────────────┐
│           模型评估与选择                  │
│  准确率/精确率/召回率/F1/混淆矩阵/CV     │
│  → 自动选择F1最高的模型作为默认模型       │
└───────────────────┬─────────────────────┘
                     ▼
              模型持久化保存
              (joblib序列化)
```

#### 阶段4：异常检测与健康评估（技术方向4）

```
新采集振动信号
    │
    ├──→ SPC控制图检测 ──→ 异常判定（3σ控制限）
    │
    ├──→ 孤立森林检测 ───→ 异常分数
    │
    └──→ 健康度评估 ─────→ 0-100分 + 5级等级
              │
              ▼
        综合诊断结果
    （故障类型 + 异常状态 + 健康度）
```

### 4.3 系统集成路线

```
┌──────────┐     RESTful API      ┌──────────────┐
│  前端页面 │ ◄──────────────────► │  Flask后端    │
│ (ECharts) │   JSON数据交互        │  (API路由)    │
└──────────┘                       └──────┬───────┘
                                            │
                              ┌─────────────┼─────────────┐
                              ▼             ▼             ▼
                        ┌──────────┐ ┌──────────┐ ┌──────────┐
                        │ 算法引擎  │ │ 业务服务  │ │ 数据访问  │
                        │ (4模块)  │ │ (3服务)  │ │ (SQLite) │
                        └──────────┘ └──────────┘ └──────────┘
```

---

## 五、计划安排

### 5.1 总体进度（10天课程设计）

| 阶段 | 时间 | 主要任务 | 交付物 |
|------|------|----------|--------|
| 第1阶段 | D1-D2 | 动员、工具配置、vibe coding学习、选题调研 | 学习笔记、调研记录 |
| 第2阶段 | D3-D5 | 选题与方案设计 | 选题说明.md、方案设计.md |
| 第3阶段 | D6 | 数据资源整理 | 数据集、预处理脚本、prompt记录 |
| 第4阶段 | D7-D8 | 详细开发（后端、算法、前端） | 完整可运行系统 |
| 第5阶段 | D9 | 集成调试、报告撰写 | 测试通过、设计报告 |
| 第6阶段 | D10 | 答辩 | PPT、演示视频、答辩 |

### 5.2 详细开发计划（第4阶段 D7-D8）

#### D7 上午：后端与算法开发
- [ ] 完成数据预处理模块开发与单元测试
- [ ] 完成特征提取模块开发与单元测试
- [ ] 完成故障分类模块开发与单元测试
- [ ] 完成异常检测模块开发与单元测试
- [ ] 完成数据库模型定义与初始化

#### D7 下午：后端API与服务开发
- [ ] 完成诊断服务层开发
- [ ] 完成数据服务层开发
- [ ] 完成模型服务层开发
- [ ] 完成诊断相关API路由
- [ ] 完成数据管理API路由
- [ ] 完成模型管理API路由
- [ ] 后端API联调测试

#### D8 上午：前端开发
- [ ] 完成基础布局模板（侧边栏+顶部导航）
- [ ] 完成仪表盘页面
- [ ] 完成在线诊断页面（上传+可视化+结果展示）
- [ ] 完成数据管理页面
- [ ] 完成模型管理页面
- [ ] 完成历史记录页面

#### D8 下午：集成与优化
- [ ] 前后端联调
- [ ] 模拟数据生成与模型训练
- [ ] 全流程测试（上传→诊断→结果展示）
- [ ] 界面美化与交互优化
- [ ] Bug修复

### 5.3 里程碑与验收节点

| 里程碑 | 时间节点 | 验收标准 |
|--------|----------|----------|
| M1：选题确认 | D3结束 | 选题说明.md提交，技术方向≥3个 |
| M2：方案完成 | D5结束（周四24:00） | 方案设计.md提交，群内接龙打卡 |
| M3：数据就绪 | D6结束 | data目录有数据集，预处理脚本可运行 |
| M4：后端完成 | D7结束 | 所有API接口可调用，算法模块测试通过 |
| M5：前端完成 | D8中午 | 所有页面可访问，前端交互正常 |
| M6：系统集成 | D8结束 | 全流程可运行，演示无阻塞 |
| M7：报告完成 | D9结束 | 设计报告提交，自动化测试通过 |
| M8：答辩 | D10 | PPT、演示视频就绪，答辩完成 |

---

## 六、参考文献

### 6.1 教材与专著

[1] 张智海, 等. 制造智能技术基础[M]. 北京: 清华大学出版社, 2022.

[2] 杨杰. 人工智能基础[M]. 北京: 机械工业出版社, 2025.

[3] 周志华. 机器学习[M]. 北京: 清华大学出版社, 2016.

[4] 何正嘉, 等. 机械故障诊断学[M]. 北京: 高等教育出版社, 2012.

[5] 屈梁生, 何正嘉. 机械故障诊断的理论与方法[M]. 北京: 机械工业出版社, 2010.

### 6.2 学术论文

[6] Lei Y, Lin J, He Z, et al. A review on empirical mode decomposition in fault diagnosis of rotating machinery[J]. Mechanical Systems and Signal Processing, 2013, 35(1-2): 108-126.

[7] Smith W A, Randall R B. Rolling element bearing diagnostics using the Case Western Reserve University data: A benchmark study[J]. Mechanical Systems and Signal Processing, 2015, 64-65: 100-131.

[8] 雷亚国, 贾峰, 孔德同, 等. 大数据下机械智能故障诊断的机遇与挑战[J]. 机械工程学报, 2018, 54(5): 94-104.

[9] 钟秉林, 黄仁. 机械故障诊断学[M]. 北京: 机械工业出版社, 1997.

[10] Samanta B, Al-Balushi K R. Artificial neural network based fault diagnostics of rolling element bearings using time-domain features[J]. Mechanical Systems and Signal Processing, 2003, 17(2): 317-328.

### 6.3 技术文档与在线资源

[11] Flask官方文档. https://flask.palletsprojects.com/

[12] scikit-learn官方文档. https://scikit-learn.org/stable/

[13] ECharts官方文档. https://echarts.apache.org/

[14] CWRU轴承数据集. https://engineering.case.edu/bearingdatacenter

[15] 和鲸社区轴承振动信号数据集. https://www.heywhale.com/mw/dataset/69a7d5b64a5a2f8d77925643

[16] PyWavelets文档. https://pywavelets.readthedocs.io/

[17] SciPy信号处理文档. https://docs.scipy.org/doc/scipy/reference/signal.html

### 6.4 vibe coding学习资源

[18] GitHub. https://github.com

[19] CSDN. https://www.csdn.net

[20] B站视频. https://www.bilibili.com

---

## 七、风险评估与应对

| 风险项 | 风险等级 | 影响 | 应对措施 |
|--------|----------|------|----------|
| 公开数据集下载失败 | 中 | 模型无法训练 | 提前准备模拟数据生成脚本，基于数学模型生成仿真数据 |
| Python依赖包安装失败 | 低 | 环境无法配置 | 提供requirements.txt，建议使用国内镜像源；准备离线安装包 |
| 算法模块Bug导致诊断不准 | 中 | 演示效果差 | 提前编写单元测试，每个算法模块独立验证；准备好预期结果 |
| 前后端联调问题 | 中 | 系统无法运行 | 提前定义API接口规范，使用Postman或curl测试接口 |
| 时间不足 | 高 | 无法完成全部功能 | 按优先级开发，核心功能（在线诊断）优先，次要功能可简化 |
| 答辩时代码讲解不清 | 中 | 影响答辩成绩 | 提前熟悉每段代码，准备代码讲解笔记；选择可解释性强的模型 |

---

## 八、AI使用披露（方案设计阶段）

### 8.1 使用的AI工具

| 工具名称 | 用途 | 使用阶段 |
|----------|------|----------|
| 豆包AI编程助手 | 需求分析、方案设计、代码生成、文档撰写 | 全程 |
| GitHub Copilot / Cursor | 代码补全、重构建议 | 开发阶段 |

### 8.2 关键Prompt策略

1. **任务拆解**：将"开发故障诊断系统"拆解为"预处理模块→特征提取→分类模型→API→前端"等子任务，逐个完成；
2. **约束说明**：明确指定技术栈（Flask+scikit-learn+ECharts）、文件结构、命名规范；
3. **验收标准**：每个模块完成后要求可运行的示例代码和预期输出；
4. **上下文工程**：提供项目规则文件（AGENTS.md），让AI先读已有代码再修改；
5. **人工审查**：AI生成代码后逐行审查，修改不符合项目规范的部分。

### 8.3 AI出错案例及纠正

（将在开发阶段记录具体案例）
